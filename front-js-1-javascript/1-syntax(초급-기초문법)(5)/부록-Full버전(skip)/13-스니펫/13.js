// vscode 기준, 확장 안깔고 처음부터 쓰는 기본 스니펫
// 그냥 쳐보기만 하자. (이 파일 실행하면 에러난다.)

// 스니펫: log
console.log();

// 스니펫: fu 두번째
function name(params) {

}

// 스니펫: 함수앞에서 /** */
/**
 * 
 * @param {*} params 
 */
function name(params) {
    
}

// 스니펫: for 두번째 
for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}

// 스니펫: forEach 두번째
array.forEach(element => {
    
});

// 스니펫: forIn 두번째
for (const key in object) {
    if (Object.hasOwnProperty.call(object, key)) {
        const element = object[key];
        
    }
}

// 스니펫: if 두번째, 세번째
if (condition) {
    
}

if (condition) {
    
} else {
    
}

// 나머지는 스니펫 확장을 깔거나, 필요한 것을 vscode 세팅에서 직접 등록해쓰거나, 검색해가면서 쓰거나 하자.
