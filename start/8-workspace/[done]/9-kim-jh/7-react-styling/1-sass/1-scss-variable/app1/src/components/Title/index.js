import React from 'react'
import Header from '../../common/Header/Header'

export default function Title () {
    const text = "안녕하세요!"
    
    return (
        <div>
            <Header text={text} />
        </div>
    )
}
