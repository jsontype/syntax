[before react]

# HTML, CSS, JS
# CMS : content management system (Figma, Wordpress)
# Git

JS : 
    spreading, destructering
    #   hashtable : map, set

# NodeJS : 
    #   webpack : bundling, loader, plugin
    #   websocket
    #   express
    #   ...
    #   -> npx create-react-app appName

React : 
    1. syntax
        알고 있는 것 : jsx, useState, useEffect
        배울 것 : props, spreading, destructering, useRef, useReducer, global state, action, performance tuning, api, router, typescript
    2. styling
        sass
        css-module
        styled-components
    3. state management
        context-api
        react-redux
        redux-saga
