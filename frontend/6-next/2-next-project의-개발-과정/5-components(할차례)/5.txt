[component]

1. 사전준비 : 프로젝트를 새로 깔지 않고, 이전 챕터에서의 코드를 그대로 카피해서 가지고 온다.

1. Server 컴포넌트 vs Client 컴포넌트
  - Server 컴포넌트
    넥스트 앱에서는 컴포넌트를 만들면 기본적으로 Server 컴포넌트가 된다.
  - Client 컴포넌트
    넥스트 앱에서는 파일 맨 위에 'use client' 넣고 만들면 Client 컴포넌트가 된다.
    넥스트가 아닌 리액트 앱에서는 'use client'가 없어도 기본적으로 Client 컴포넌트이다.

1. src/app/list/Title.js : 컴포넌트 추가
  import React from "react"
  import styles from "./page.module.css"

  export default function Title() {
    return <h2 className={styles.title}>상품목록</h2>
  }

1. src/app/list/page.js 수정 : 컴포넌트 추가




1. 테스트
  $ cd app1
  $ npm run dev
  http://localhost:3000/
