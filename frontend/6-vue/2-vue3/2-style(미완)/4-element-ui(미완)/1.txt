[ElementUI]

1. 사전준비 : init 앱을 가지고 온다.

1. 사전준비 : src/components/HelloWorld.vue 삭제

1. src/components/buttons/RoundButton.js
<template>
  <Button variant="contained" color="primary" size="large" :disabled="disabled" :onClick="onClick">
    {{ label }}
  </Button>
</template>

<script setup>
import Button from '@element-ui/core/Button';

defaultProps: {
  label: {
    type: String,
    default: 'Button',
  },
  disabled: {
    type: Boolean,
    default: false,
  },
  onClick: {
    type: Function,
    default: () => {},
  },
}
</script>
