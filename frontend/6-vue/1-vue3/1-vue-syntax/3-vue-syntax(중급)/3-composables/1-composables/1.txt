[Composables]

1. Composables(= Custom Hooks)란?
  재사용 가능한 로직을 별도의 훅 함수(use~)로 만든 것을 말하며, 여러 컴포넌트에서 동일한 기능을 사용할 때 유용하다.
  일반적으로 "hooks" 또는 "composables" 라는 폴더 안에 모아둔다.
  목적은 UI와 로직을 분리해서, 로직을 보다 더 관리하기 쉽게 하는 것이다.
