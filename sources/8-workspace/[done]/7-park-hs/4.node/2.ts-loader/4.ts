// // 함수 파라미터, 반환값 타입 표기
// function sum(a: number, b: number): number {
//     return a + b // + 'b' 를 붙이면 에러. 반환값도 number이기 때문.
// }
  
// // 함수 옵셔널 파라미터 : 옵셔널한 파라미터의 경우, ?를 붙이면 된다. 반환값이 없으면, 반환값 타입에 void를 입력한다.
// function log(a: string, b?: string): void {
//     console.log(a)
//     if (b) { console.log(b) }
// }
// log('hello')
// log('hello', 'typescript')
