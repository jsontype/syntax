function clicked() {
    // 변수 : 변하는 수
    // 변수(variables)의 선언 : var, let(변하는 변수), const(상수)
    // let a = 1
    // a = 2
    // const b = 1
    // console.log(a)
    // console.log(b)

    // 변수의 타입(type)
    // * undefined : 아예 변수가 없는 것
    // null : 변수가 비어있는 것
    // number : 수
    // string : 문자열, 연산 불가
    // boolean : 논리, true, false
    // array : 배열

    // let a = 5
    // console.log(c * 5) // 곱
    // console.log(c / 5) // 나누기
    // console.log(c ** 5) // 승
    // console.log(c % 2) // 나머지
    // console.log(parseInt(c / 2)) // 몫

    // let a = "1"
    // console.log(a+1)

    // let a = 3
    // let b = Boolean(a > 1)
    // console.log(b)
    let c = null // 값이 있는 것 true 값이 없는 것(=null) false
    console.log(Boolean(c))
    // 0(심지만 있음)과 null(심지도 없음)은 다르다.
}